box: debian
build:
  steps:
    - install-packages:
        packages: git ssh-client
    - script:
        name: initialize and update git submodules
        code: |
            git submodule init
            git submodule update --remote --recursive
    - arjen/hugo-build:
        version: "HEAD"
        theme: academic
    - script:
        name: ls stuff
        code: |
          ls -haltr /pipeline/source/public
    
deploy:
  steps:
    - script:
        name: ls output
        code: |
          ls -haltr /pipeline/output
    - script:
        name: ls source
        code: |
          ls -haltr /pipeline/source
    - install-packages:
        packages: liberror-perl glibc
    - pahnev/gh-pages@0.3.1:
        token: $GIT_TOKEN
        domain: hugo-wercker.ig.nore.me
        basedir: public